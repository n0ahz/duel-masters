Add mongo db support to the project. The database will contain user information, including their username, email, and password. The project should be able to perform basic CRUD operations on the user data, such as creating a new user, reading user information, updating user details, and deleting a user from the database. Additionally, implement authentication functionality to allow users to log in and access their information securely. User will be able to register and login using Google OAuth, and the system should handle the authentication flow accordingly. Ensure that the user data is stored securely, and implement necessary measures to protect against common security vulnerabilities. Also create an admin user who does not need to register and can access all user information. The admin user should have a predefined username and password that is securely stored in the database. Admin will login using the username and password.

The database will also have information of all the cards. Card schema is provided in the data directory at project root.
Each user will be able to create Decks, which are collection of the cards. Deck will consist of maximum 40 cards with each card having a count of maximum 4. Create mongo migration script to add the card information to the database. The migration script should read the card data from the provided schema and insert it into the appropriate collection in the MongoDB database. This will ensure that all the card information is readily available for users when they create their decks and play games. The card information will include details such as the card name, type, attributes, and any special abilities or effects associated with the card. This data will be essential for users to make informed decisions when building their decks and strategizing during gameplay.

Also the database will game information of the Games. When a game is created, besides the socket, the information will be stored in the db. Each step of the game, all the chat logs, moves, and actions will be stored in the database. This will allow for game history tracking and analysis. The game information will include details such as the players involved, the cards played, the outcome of the game, and any relevant timestamps. This data can be used for various purposes, such as generating statistics, providing insights into player behavior, and improving the overall gaming experience.
